Note: For simplicity's sake, both template and rendering pages are contained within the same folder.
In a real application, these should be separate so that a visitor would be unable to access an
empty template page.
